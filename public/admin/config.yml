backend:
  name: github
  repo: alexisarfn/Alethig-Media
  branch: main
  # Proxy OAuth desplegado en Cloudflare Workers (ver carpeta oauth-worker/).
  base_url: https://decap-oauth.alexisfuel93.workers.dev
  auth_endpoint: auth

local_backend: true

media_folder: "public/uploads"
public_folder: "/uploads"

locale: "es"

collections:
  # ─────────────── AJUSTES ───────────────
  # Datos de marca compartidos (hechos, no se traducen) separados de los
  # textos de navegación/footer (traducibles) — ver src/content/settings/.
  - name: settings
    label: "Ajustes del sitio"
    editor:
      preview: false
    files:
      - name: site
        label: "Marca (compartido, no se traduce)"
        file: "src/content/settings/site.json"
        format: json
        fields:
          - { name: brand, label: "Nombre de marca", widget: string }
          - name: contact
            label: "Datos de contacto"
            widget: object
            fields:
              - { name: email, label: "Email", widget: string }
              - { name: location, label: "Ubicación", widget: string }
          - name: whatsapp
            label: "WhatsApp"
            widget: object
            required: false
            fields:
              - { name: number, label: "Número (solo dígitos, con código de país)", widget: string }
              - { name: display, label: "Número a mostrar", widget: string }
          - name: social
            label: "Redes sociales"
            widget: object
            required: false
            fields:
              - { name: instagram, label: "Instagram (URL)", widget: string, required: false }
              - { name: facebook, label: "Facebook (URL)", widget: string, required: false }
              - { name: tiktok, label: "TikTok (URL)", widget: string, required: false }

      - name: site-es
        label: "Navegación y footer (Español)"
        file: "src/content/settings/site.es.json"
        format: json
        fields:
          - { name: ctaLabel, label: "Texto botón CTA", widget: string }
          - name: nav
            label: "Menú de navegación"
            widget: list
            fields:
              - { name: label, label: "Texto", widget: string }
              - { name: href, label: "Enlace", widget: string }
          - { name: contactHours, label: "Horario de contacto", widget: string }
          - { name: whatsappMessage, label: "WhatsApp · Mensaje pre-escrito", widget: string }
          - { name: footerTagline, label: "Eslogan del footer", widget: text }
          - name: footerPages
            label: "Footer — Páginas"
            widget: list
            fields:
              - { name: label, label: "Texto", widget: string }
              - { name: href, label: "Enlace", widget: string }
          - name: footerLinks
            label: "Footer — Enlaces"
            widget: list
            fields:
              - { name: label, label: "Texto", widget: string }
              - { name: href, label: "Enlace", widget: string }
          - name: footerBottomLinks
            label: "Footer — Enlaces inferiores"
            widget: list
            fields:
              - { name: label, label: "Texto", widget: string }
              - { name: href, label: "Enlace", widget: string }
          - { name: copyright, label: "Copyright", widget: string }

      - name: site-en
        label: "Navegación y footer (Inglés)"
        file: "src/content/settings/site.en.json"
        format: json
        fields:
          - { name: ctaLabel, label: "Texto botón CTA", widget: string }
          - name: nav
            label: "Menú de navegación"
            widget: list
            fields:
              - { name: label, label: "Texto", widget: string }
              - { name: href, label: "Enlace", widget: string }
          - { name: contactHours, label: "Horario de contacto", widget: string }
          - { name: whatsappMessage, label: "WhatsApp · Mensaje pre-escrito", widget: string }
          - { name: footerTagline, label: "Eslogan del footer", widget: text }
          - name: footerPages
            label: "Footer — Páginas"
            widget: list
            fields:
              - { name: label, label: "Texto", widget: string }
              - { name: href, label: "Enlace", widget: string }
          - name: footerLinks
            label: "Footer — Enlaces"
            widget: list
            fields:
              - { name: label, label: "Texto", widget: string }
              - { name: href, label: "Enlace", widget: string }
          - name: footerBottomLinks
            label: "Footer — Enlaces inferiores"
            widget: list
            fields:
              - { name: label, label: "Texto", widget: string }
              - { name: href, label: "Enlace", widget: string }
          - { name: copyright, label: "Copyright", widget: string }

  # ─────────────── PÁGINAS ───────────────
  - name: pages
    label: "Páginas"
    editor:
      preview: false
    files:
      - name: home
        label: "Inicio"
        file: "src/content/pages/home.es.json"
        format: json
        fields:
          - { name: title, label: "Título de pestaña (SEO)", widget: string }
          - { name: heroTag, label: "Hero · Etiqueta", widget: string }
          - { name: heroHeading, label: "Hero · Titular", widget: string }
          - { name: heroHighlight, label: "Hero · Palabra resaltada", widget: string, required: false }
          - { name: heroParagraph, label: "Hero · Párrafo", widget: text }
          - { name: heroCtaPrimary, label: "Hero · Botón principal", widget: object, fields: [ { name: label, widget: string }, { name: href, widget: string } ] }
          - { name: heroCtaSecondary, label: "Hero · Botón secundario", widget: object, fields: [ { name: label, widget: string }, { name: href, widget: string } ] }
          - { name: heroImage, label: "Hero · Imagen", widget: image }
          - { name: heroImageAlt, label: "Hero · Texto alternativo", widget: string }
          - name: trustBar
            label: "Barra de confianza (debajo del hero)"
            widget: list
            fields:
              - { name: icon, label: "Icono", widget: string }
              - { name: label, label: "Texto", widget: string }
          - { name: businessPathsTag, label: "Líneas de negocio · Etiqueta", widget: string }
          - { name: businessPathsHeading, label: "Líneas de negocio · Titular", widget: string }
          - name: businessPaths
            label: "Líneas de negocio (3 tarjetas)"
            widget: list
            fields:
              - { name: num, label: "Número (01, 02, 03)", widget: string }
              - { name: icon, label: "Icono", widget: string }
              - { name: headline, label: "Pregunta / titular de la tarjeta", widget: string }
              - { name: title, label: "Nombre de la línea de negocio", widget: string }
              - { name: items, label: "Ítems de apoyo", widget: list, field: { name: item, label: "Ítem", widget: string } }
              - { name: desc, label: "Descripción corta", widget: text }
              - { name: ctaLabel, label: "Texto del botón", widget: string }
              - { name: ctaHref, label: "Enlace (ancla #... o ruta existente)", widget: string }
          - { name: trafficTag, label: "Traffic & Growth · Etiqueta", widget: string }
          - { name: trafficHeading, label: "Traffic & Growth · Titular", widget: string }
          - { name: trafficIntro, label: "Traffic & Growth · Párrafo", widget: text }
          - name: trafficModules
            label: "Traffic & Growth · Módulos"
            widget: list
            fields:
              - { name: icon, label: "Icono", widget: string }
              - { name: title, label: "Título", widget: string }
              - { name: desc, label: "Descripción", widget: text }
          - { name: trafficCtaLabel, label: "Traffic & Growth · Texto botón", widget: string }
          - { name: osTag, label: "Alethig Media OS · Etiqueta", widget: string }
          - { name: osHeading, label: "Alethig Media OS · Titular", widget: string }
          - { name: osHighlight, label: "Alethig Media OS · Palabra resaltada", widget: string, required: false }
          - { name: osBody, label: "Alethig Media OS · Párrafo", widget: text }
          - { name: osCtaLabel, label: "Alethig Media OS · Texto botón", widget: string }
          - { name: sublimationTag, label: "Sublimación · Etiqueta", widget: string }
          - { name: sublimationHeading, label: "Sublimación · Titular", widget: string }
          - { name: sublimationIntro, label: "Sublimación · Párrafo", widget: text }
          - { name: sublimationCategories, label: "Sublimación · Categorías", widget: list, field: { name: category, label: "Categoría", widget: string } }
          - { name: sublimationCtaLabel, label: "Sublimación · Texto botón", widget: string }
          - { name: servicesTag, label: "Servicios de apoyo · Etiqueta", widget: string }
          - { name: servicesHeading, label: "Servicios de apoyo · Titular", widget: string }
          - { name: servicesHighlight, label: "Servicios de apoyo · Palabra resaltada (sin uso actualmente)", widget: string, required: false }
          - { name: servicesSubtitle, label: "Servicios de apoyo · Subtítulo", widget: text }
          - name: services
            label: "Servicios de apoyo (tarjetas del home)"
            widget: list
            fields:
              - { name: icon, label: "Icono", widget: string }
              - { name: title, label: "Título", widget: string }
              - { name: desc, label: "Descripción", widget: text }
              - { name: href, label: "Enlace", widget: string }
          - { name: reasonsTag, label: "Por qué · Etiqueta", widget: string }
          - { name: reasonsHeading, label: "Por qué · Titular", widget: string }
          - { name: reasonsHighlight, label: "Por qué · Palabra resaltada", widget: string, required: false }
          - name: reasons
            label: "Razones"
            widget: list
            fields:
              - { name: icon, label: "Icono", widget: string }
              - { name: title, label: "Título", widget: string }
              - { name: desc, label: "Descripción", widget: text }
          - { name: processTag, label: "Proceso · Etiqueta", widget: string }
          - { name: processHeading, label: "Proceso · Titular", widget: string }
          - name: processSteps
            label: "Proceso · Pasos"
            widget: list
            fields:
              - { name: num, label: "Número (01, 02…)", widget: string }
              - { name: title, label: "Título", widget: string }
              - { name: desc, label: "Descripción", widget: text }
          - { name: ctaTag, label: "CTA · Etiqueta", widget: string }
          - { name: ctaHeading, label: "CTA · Titular", widget: string }
          - { name: ctaHighlight, label: "CTA · Palabra resaltada", widget: string, required: false }
          - { name: ctaParagraph, label: "CTA · Párrafo", widget: text }
          - { name: ctaButtonLabel, label: "CTA · Texto botón", widget: string }
          - { name: ctaButtonIcon, label: "CTA · Icono botón", widget: string }

      - name: home-en
        label: "Inicio (Inglés)"
        file: "src/content/pages/home.en.json"
        format: json
        fields:
          - { name: title, label: "Título de pestaña (SEO)", widget: string }
          - { name: heroTag, label: "Hero · Etiqueta", widget: string }
          - { name: heroHeading, label: "Hero · Titular", widget: string }
          - { name: heroHighlight, label: "Hero · Palabra resaltada", widget: string, required: false }
          - { name: heroParagraph, label: "Hero · Párrafo", widget: text }
          - { name: heroCtaPrimary, label: "Hero · Botón principal", widget: object, fields: [ { name: label, widget: string }, { name: href, widget: string } ] }
          - { name: heroCtaSecondary, label: "Hero · Botón secundario", widget: object, fields: [ { name: label, widget: string }, { name: href, widget: string } ] }
          - { name: heroImage, label: "Hero · Imagen", widget: image }
          - { name: heroImageAlt, label: "Hero · Texto alternativo", widget: string }
          - name: trustBar
            label: "Barra de confianza (debajo del hero)"
            widget: list
            fields:
              - { name: icon, label: "Icono", widget: string }
              - { name: label, label: "Texto", widget: string }
          - { name: businessPathsTag, label: "Líneas de negocio · Etiqueta", widget: string }
          - { name: businessPathsHeading, label: "Líneas de negocio · Titular", widget: string }
          - name: businessPaths
            label: "Líneas de negocio (3 tarjetas)"
            widget: list
            fields:
              - { name: num, label: "Número (01, 02, 03)", widget: string }
              - { name: icon, label: "Icono", widget: string }
              - { name: headline, label: "Pregunta / titular de la tarjeta", widget: string }
              - { name: title, label: "Nombre de la línea de negocio", widget: string }
              - { name: items, label: "Ítems de apoyo", widget: list, field: { name: item, label: "Ítem", widget: string } }
              - { name: desc, label: "Descripción corta", widget: text }
              - { name: ctaLabel, label: "Texto del botón", widget: string }
              - { name: ctaHref, label: "Enlace (ancla #... o ruta existente)", widget: string }
          - { name: trafficTag, label: "Traffic & Growth · Etiqueta", widget: string }
          - { name: trafficHeading, label: "Traffic & Growth · Titular", widget: string }
          - { name: trafficIntro, label: "Traffic & Growth · Párrafo", widget: text }
          - name: trafficModules
            label: "Traffic & Growth · Módulos"
            widget: list
            fields:
              - { name: icon, label: "Icono", widget: string }
              - { name: title, label: "Título", widget: string }
              - { name: desc, label: "Descripción", widget: text }
          - { name: trafficCtaLabel, label: "Traffic & Growth · Texto botón", widget: string }
          - { name: osTag, label: "Alethig Media OS · Etiqueta", widget: string }
          - { name: osHeading, label: "Alethig Media OS · Titular", widget: string }
          - { name: osHighlight, label: "Alethig Media OS · Palabra resaltada", widget: string, required: false }
          - { name: osBody, label: "Alethig Media OS · Párrafo", widget: text }
          - { name: osCtaLabel, label: "Alethig Media OS · Texto botón", widget: string }
          - { name: sublimationTag, label: "Sublimación · Etiqueta", widget: string }
          - { name: sublimationHeading, label: "Sublimación · Titular", widget: string }
          - { name: sublimationIntro, label: "Sublimación · Párrafo", widget: text }
          - { name: sublimationCategories, label: "Sublimación · Categorías", widget: list, field: { name: category, label: "Categoría", widget: string } }
          - { name: sublimationCtaLabel, label: "Sublimación · Texto botón", widget: string }
          - { name: servicesTag, label: "Servicios de apoyo · Etiqueta", widget: string }
          - { name: servicesHeading, label: "Servicios de apoyo · Titular", widget: string }
          - { name: servicesHighlight, label: "Servicios de apoyo · Palabra resaltada (sin uso actualmente)", widget: string, required: false }
          - { name: servicesSubtitle, label: "Servicios de apoyo · Subtítulo", widget: text }
          - name: services
            label: "Servicios de apoyo (tarjetas del home)"
            widget: list
            fields:
              - { name: icon, label: "Icono", widget: string }
              - { name: title, label: "Título", widget: string }
              - { name: desc, label: "Descripción", widget: text }
              - { name: href, label: "Enlace", widget: string }
          - { name: reasonsTag, label: "Por qué · Etiqueta", widget: string }
          - { name: reasonsHeading, label: "Por qué · Titular", widget: string }
          - { name: reasonsHighlight, label: "Por qué · Palabra resaltada", widget: string, required: false }
          - name: reasons
            label: "Razones"
            widget: list
            fields:
              - { name: icon, label: "Icono", widget: string }
              - { name: title, label: "Título", widget: string }
              - { name: desc, label: "Descripción", widget: text }
          - { name: processTag, label: "Proceso · Etiqueta", widget: string }
          - { name: processHeading, label: "Proceso · Titular", widget: string }
          - name: processSteps
            label: "Proceso · Pasos"
            widget: list
            fields:
              - { name: num, label: "Número (01, 02…)", widget: string }
              - { name: title, label: "Título", widget: string }
              - { name: desc, label: "Descripción", widget: text }
          - { name: ctaTag, label: "CTA · Etiqueta", widget: string }
          - { name: ctaHeading, label: "CTA · Titular", widget: string }
          - { name: ctaHighlight, label: "CTA · Palabra resaltada", widget: string, required: false }
          - { name: ctaParagraph, label: "CTA · Párrafo", widget: text }
          - { name: ctaButtonLabel, label: "CTA · Texto botón", widget: string }
          - { name: ctaButtonIcon, label: "CTA · Icono botón", widget: string }

      - name: about
        label: "Nosotros"
        file: "src/content/pages/about.es.json"
        format: json
        fields:
          - { name: title, label: "Título de pestaña (SEO)", widget: string }
          - { name: heroTag, label: "Hero · Etiqueta", widget: string }
          - { name: heroHeading, label: "Hero · Titular", widget: string }
          - { name: heroHighlight, label: "Hero · Palabra resaltada", widget: string, required: false }
          - { name: heroParagraph, label: "Hero · Párrafo", widget: text }
          - { name: heroCtaPrimary, label: "Hero · Botón principal", widget: object, fields: [ { name: label, widget: string }, { name: href, widget: string } ] }
          - { name: heroCtaSecondary, label: "Hero · Botón secundario", widget: object, fields: [ { name: label, widget: string }, { name: href, widget: string } ] }
          - { name: heroImage, label: "Hero · Imagen", widget: image }
          - { name: heroImageAlt, label: "Hero · Texto alternativo", widget: string }
          - { name: aboutTag, label: "Esencia · Etiqueta", widget: string }
          - { name: aboutHeading, label: "Esencia · Titular", widget: string }
          - { name: aboutHighlight, label: "Esencia · Palabra resaltada", widget: string, required: false }
          - { name: aboutImage, label: "Esencia · Imagen", widget: image }
          - { name: aboutImageAlt, label: "Esencia · Texto alternativo", widget: string }
          - { name: aboutBadgeValue, label: "Insignia · Valor", widget: string }
          - { name: aboutBadgeLabel, label: "Insignia · Etiqueta", widget: string }
          - { name: aboutParagraphs, label: "Esencia · Párrafos", widget: list, field: { name: text, label: "Párrafo", widget: text } }
          - { name: aboutCtaLabel, label: "Esencia · Texto botón", widget: string }
          - { name: statsTag, label: "Métricas · Etiqueta", widget: string }
          - { name: statsHeading, label: "Métricas · Titular", widget: string }
          - { name: statsHighlight, label: "Métricas · Palabra resaltada", widget: string, required: false }
          - { name: statsParagraph, label: "Métricas · Párrafo", widget: text }
          - name: stats
            label: "Métricas"
            widget: list
            fields:
              - { name: value, label: "Valor", widget: string }
              - { name: label, label: "Etiqueta", widget: string }
          - { name: teamTag, label: "Equipo · Etiqueta", widget: string }
          - { name: teamHeading, label: "Equipo · Titular", widget: string }
          - { name: teamHighlight, label: "Equipo · Palabra resaltada", widget: string, required: false }
          - { name: teamQuote, label: "Equipo · Cita", widget: string }
          - { name: teamCtaLabel, label: "Equipo · Texto botón", widget: string }
          - name: team
            label: "Equipo"
            widget: list
            fields:
              - { name: name, label: "Nombre", widget: string }
              - { name: role, label: "Cargo", widget: string }
              - { name: img, label: "Foto", widget: image }

      - name: about-en
        label: "Nosotros (Inglés)"
        file: "src/content/pages/about.en.json"
        format: json
        fields:
          - { name: title, label: "Título de pestaña (SEO)", widget: string }
          - { name: heroTag, label: "Hero · Etiqueta", widget: string }
          - { name: heroHeading, label: "Hero · Titular", widget: string }
          - { name: heroHighlight, label: "Hero · Palabra resaltada", widget: string, required: false }
          - { name: heroParagraph, label: "Hero · Párrafo", widget: text }
          - { name: heroCtaPrimary, label: "Hero · Botón principal", widget: object, fields: [ { name: label, widget: string }, { name: href, widget: string } ] }
          - { name: heroCtaSecondary, label: "Hero · Botón secundario", widget: object, fields: [ { name: label, widget: string }, { name: href, widget: string } ] }
          - { name: heroImage, label: "Hero · Imagen", widget: image }
          - { name: heroImageAlt, label: "Hero · Texto alternativo", widget: string }
          - { name: aboutTag, label: "Esencia · Etiqueta", widget: string }
          - { name: aboutHeading, label: "Esencia · Titular", widget: string }
          - { name: aboutHighlight, label: "Esencia · Palabra resaltada", widget: string, required: false }
          - { name: aboutImage, label: "Esencia · Imagen", widget: image }
          - { name: aboutImageAlt, label: "Esencia · Texto alternativo", widget: string }
          - { name: aboutBadgeValue, label: "Insignia · Valor", widget: string }
          - { name: aboutBadgeLabel, label: "Insignia · Etiqueta", widget: string }
          - { name: aboutParagraphs, label: "Esencia · Párrafos", widget: list, field: { name: text, label: "Párrafo", widget: text } }
          - { name: aboutCtaLabel, label: "Esencia · Texto botón", widget: string }
          - { name: statsTag, label: "Métricas · Etiqueta", widget: string }
          - { name: statsHeading, label: "Métricas · Titular", widget: string }
          - { name: statsHighlight, label: "Métricas · Palabra resaltada", widget: string, required: false }
          - { name: statsParagraph, label: "Métricas · Párrafo", widget: text }
          - name: stats
            label: "Métricas"
            widget: list
            fields:
              - { name: value, label: "Valor", widget: string }
              - { name: label, label: "Etiqueta", widget: string }
          - { name: teamTag, label: "Equipo · Etiqueta", widget: string }
          - { name: teamHeading, label: "Equipo · Titular", widget: string }
          - { name: teamHighlight, label: "Equipo · Palabra resaltada", widget: string, required: false }
          - { name: teamQuote, label: "Equipo · Cita", widget: string }
          - { name: teamCtaLabel, label: "Equipo · Texto botón", widget: string }
          - name: team
            label: "Equipo"
            widget: list
            fields:
              - { name: name, label: "Nombre", widget: string }
              - { name: role, label: "Cargo", widget: string }
              - { name: img, label: "Foto", widget: image }

      - name: contact
        label: "Contacto"
        file: "src/content/pages/contact.es.json"
        format: json
        fields:
          - { name: title, label: "Título de pestaña (SEO)", widget: string }
          - { name: heroTag, label: "Hero · Etiqueta", widget: string }
          - { name: heroHeading, label: "Hero · Titular", widget: string }
          - { name: heroHighlight, label: "Hero · Palabra resaltada", widget: string, required: false }
          - { name: heroParagraph, label: "Hero · Párrafo", widget: text }
          - { name: heroImage, label: "Hero · Imagen", widget: image }
          - { name: heroImageAlt, label: "Hero · Texto alternativo", widget: string }
          - { name: formInfoHeading, label: "Formulario · Título", widget: string }
          - { name: formInfoText, label: "Formulario · Texto", widget: text }
          - { name: infoEmailLabel, label: "Info · Etiqueta email", widget: string }
          - { name: infoPhoneLabel, label: "Info · Etiqueta teléfono", widget: string }
          - { name: infoHoursLabel, label: "Info · Etiqueta horario", widget: string }
          - { name: socialHeading, label: "Info · Título redes sociales", widget: string }
          - { name: submitLabel, label: "Formulario · Texto botón", widget: string }
          - name: serviceOptions
            label: "Opciones del select"
            widget: list
            fields:
              - { name: value, label: "Valor", widget: string }
              - { name: label, label: "Etiqueta", widget: string }
          - { name: mapHeading, label: "Mapa · Título", widget: string }
          - { name: mapText, label: "Mapa · Texto", widget: string }
          - name: form
            label: "Formulario · Campos y mensajes"
            widget: object
            fields:
              - { name: nameLabel, label: "Nombre · Etiqueta", widget: string }
              - { name: namePlaceholder, label: "Nombre · Placeholder", widget: string }
              - { name: businessLabel, label: "Negocio · Etiqueta", widget: string }
              - { name: businessPlaceholder, label: "Negocio · Placeholder", widget: string }
              - { name: phoneLabel, label: "Teléfono · Etiqueta", widget: string }
              - { name: phonePlaceholder, label: "Teléfono · Placeholder", widget: string }
              - { name: emailLabel, label: "Email · Etiqueta", widget: string }
              - { name: emailPlaceholder, label: "Email · Placeholder", widget: string }
              - { name: serviceLabel, label: "Servicio · Etiqueta", widget: string }
              - { name: servicePlaceholder, label: "Servicio · Placeholder (opción vacía)", widget: string }
              - { name: messageLabel, label: "Mensaje · Etiqueta", widget: string }
              - { name: messagePlaceholder, label: "Mensaje · Placeholder", widget: string }
              - { name: errorMessage, label: "Mensaje de error", widget: string }
              - { name: successHeading, label: "Éxito · Titular", widget: string }
              - { name: successText, label: "Éxito · Texto", widget: string }
          - { name: faqHeading, label: "FAQ · Titular", widget: string }
          - name: faq
            label: "FAQ · Preguntas"
            widget: list
            fields:
              - { name: question, label: "Pregunta", widget: string }
              - { name: answer, label: "Respuesta", widget: text }

      - name: contact-en
        label: "Contacto (Inglés)"
        file: "src/content/pages/contact.en.json"
        format: json
        fields:
          - { name: title, label: "Título de pestaña (SEO)", widget: string }
          - { name: heroTag, label: "Hero · Etiqueta", widget: string }
          - { name: heroHeading, label: "Hero · Titular", widget: string }
          - { name: heroHighlight, label: "Hero · Palabra resaltada", widget: string, required: false }
          - { name: heroParagraph, label: "Hero · Párrafo", widget: text }
          - { name: heroImage, label: "Hero · Imagen", widget: image }
          - { name: heroImageAlt, label: "Hero · Texto alternativo", widget: string }
          - { name: formInfoHeading, label: "Formulario · Título", widget: string }
          - { name: formInfoText, label: "Formulario · Texto", widget: text }
          - { name: infoEmailLabel, label: "Info · Etiqueta email", widget: string }
          - { name: infoPhoneLabel, label: "Info · Etiqueta teléfono", widget: string }
          - { name: infoHoursLabel, label: "Info · Etiqueta horario", widget: string }
          - { name: socialHeading, label: "Info · Título redes sociales", widget: string }
          - { name: submitLabel, label: "Formulario · Texto botón", widget: string }
          - name: serviceOptions
            label: "Opciones del select"
            widget: list
            fields:
              - { name: value, label: "Valor", widget: string }
              - { name: label, label: "Etiqueta", widget: string }
          - { name: mapHeading, label: "Mapa · Título", widget: string }
          - { name: mapText, label: "Mapa · Texto", widget: string }
          - name: form
            label: "Formulario · Campos y mensajes"
            widget: object
            fields:
              - { name: nameLabel, label: "Nombre · Etiqueta", widget: string }
              - { name: namePlaceholder, label: "Nombre · Placeholder", widget: string }
              - { name: businessLabel, label: "Negocio · Etiqueta", widget: string }
              - { name: businessPlaceholder, label: "Negocio · Placeholder", widget: string }
              - { name: phoneLabel, label: "Teléfono · Etiqueta", widget: string }
              - { name: phonePlaceholder, label: "Teléfono · Placeholder", widget: string }
              - { name: emailLabel, label: "Email · Etiqueta", widget: string }
              - { name: emailPlaceholder, label: "Email · Placeholder", widget: string }
              - { name: serviceLabel, label: "Servicio · Etiqueta", widget: string }
              - { name: servicePlaceholder, label: "Servicio · Placeholder (opción vacía)", widget: string }
              - { name: messageLabel, label: "Mensaje · Etiqueta", widget: string }
              - { name: messagePlaceholder, label: "Mensaje · Placeholder", widget: string }
              - { name: errorMessage, label: "Mensaje de error", widget: string }
              - { name: successHeading, label: "Éxito · Titular", widget: string }
              - { name: successText, label: "Éxito · Texto", widget: string }
          - { name: faqHeading, label: "FAQ · Titular", widget: string }
          - name: faq
            label: "FAQ · Preguntas"
            widget: list
            fields:
              - { name: question, label: "Pregunta", widget: string }
              - { name: answer, label: "Respuesta", widget: text }

      - name: gracias
        label: "Gracias"
        file: "src/content/pages/gracias.es.json"
        format: json
        fields:
          - { name: title, label: "Título de pestaña (SEO)", widget: string }
          - { name: heading, label: "Titular", widget: string }
          - { name: paragraph, label: "Párrafo", widget: text }
          - { name: ctaLabel, label: "Texto botón", widget: string }

      - name: gracias-en
        label: "Gracias (Inglés / Thank You)"
        file: "src/content/pages/gracias.en.json"
        format: json
        fields:
          - { name: title, label: "Título de pestaña (SEO)", widget: string }
          - { name: heading, label: "Titular", widget: string }
          - { name: paragraph, label: "Párrafo", widget: text }
          - { name: ctaLabel, label: "Texto botón", widget: string }

      - name: alethig-media-os
        label: "Alethig Media OS"
        file: "src/content/pages/alethig-media-os.es.json"
        format: json
        fields:
          - { name: title, label: "Título de pestaña (SEO)", widget: string }
          - { name: metaDescription, label: "Meta descripción (SEO)", widget: text }
          - { name: heroTag, label: "Etiqueta", widget: string }
          - { name: heading, label: "Titular", widget: string }
          - { name: highlight, label: "Palabra resaltada", widget: string, required: false }
          - { name: paragraph, label: "Párrafo", widget: text }
          - { name: ctaLabel, label: "Botón principal (abre app.alethigmedia.com)", widget: string }
          - { name: ctaSecondaryLabel, label: "Botón secundario", widget: string }

      - name: alethig-media-os-en
        label: "Alethig Media OS (Inglés)"
        file: "src/content/pages/alethig-media-os.en.json"
        format: json
        fields:
          - { name: title, label: "Título de pestaña (SEO)", widget: string }
          - { name: metaDescription, label: "Meta descripción (SEO)", widget: text }
          - { name: heroTag, label: "Etiqueta", widget: string }
          - { name: heading, label: "Titular", widget: string }
          - { name: highlight, label: "Palabra resaltada", widget: string, required: false }
          - { name: paragraph, label: "Párrafo", widget: text }
          - { name: ctaLabel, label: "Botón principal (abre app.alethigmedia.com)", widget: string }
          - { name: ctaSecondaryLabel, label: "Botón secundario", widget: string }

      # POSITIONING PHASE 3 — página bespoke dedicada de Traffic & Growth.
      - name: traffic-growth
        label: "Traffic & Growth (página dedicada)"
        file: "src/content/pages/traffic-growth.es.json"
        format: json
        fields:
          - { name: title, label: "Título de pestaña (SEO)", widget: string }
          - { name: metaDescription, label: "Meta descripción (SEO)", widget: text }
          - { name: heroTag, label: "Hero · Etiqueta", widget: string }
          - { name: heroHeading, label: "Hero · Titular", widget: string }
          - { name: heroHighlight, label: "Hero · Palabra resaltada", widget: string, required: false }
          - { name: heroParagraph, label: "Hero · Párrafo", widget: text }
          - { name: heroCtaPrimaryLabel, label: "Hero · Botón principal", widget: string }
          - { name: heroCtaPrimaryHref, label: "Hero · Enlace botón principal", widget: string }
          - { name: heroCtaSecondaryLabel, label: "Hero · Botón secundario", widget: string }
          - { name: heroCtaSecondaryHref, label: "Hero · Enlace botón secundario", widget: string }
          - { name: modulesTag, label: "Módulos · Etiqueta", widget: string }
          - { name: modulesHeading, label: "Módulos · Titular", widget: string }
          - { name: modulesIntro, label: "Módulos · Párrafo", widget: text }
          - name: modules
            label: "Módulos (6: Meta Ads, Google Ads, SEO, SEO Local, Lead Gen, Growth Strategy)"
            widget: list
            fields:
              - { name: num, label: "Número (01, 02…)", widget: string }
              - { name: icon, label: "Icono", widget: string }
              - { name: title, label: "Título", widget: string }
              - { name: desc, label: "Descripción", widget: text }
              - { name: benefit, label: "Beneficio práctico", widget: text }
          - { name: processTag, label: "Proceso · Etiqueta", widget: string }
          - { name: processHeading, label: "Proceso · Titular", widget: string }
          - name: processSteps
            label: "Proceso · Pasos"
            widget: list
            fields:
              - { name: num, label: "Número (01, 02…)", widget: string }
              - { name: title, label: "Título", widget: string }
              - { name: desc, label: "Descripción", widget: text }
          - { name: fitTag, label: "Para quién · Etiqueta", widget: string }
          - { name: fitHeading, label: "Para quién · Titular", widget: string }
          - { name: fitIntro, label: "Para quién · Línea de posicionamiento local/global", widget: text }
          - name: fitItems
            label: "Para quién · Perfiles de negocio"
            widget: list
            fields:
              - { name: icon, label: "Icono", widget: string }
              - { name: title, label: "Título", widget: string }
              - { name: desc, label: "Descripción", widget: text }
          - { name: supportTag, label: "Servicios de apoyo · Etiqueta", widget: string }
          - { name: supportHeading, label: "Servicios de apoyo · Titular", widget: string }
          - { name: supportIntro, label: "Servicios de apoyo · Párrafo", widget: text }
          - name: supportItems
            label: "Servicios de apoyo · Enlaces (solo rutas reales existentes)"
            widget: list
            fields:
              - { name: icon, label: "Icono", widget: string }
              - { name: title, label: "Título", widget: string }
              - { name: href, label: "Enlace", widget: string }
          - { name: ctaTag, label: "CTA final · Etiqueta", widget: string }
          - { name: ctaHeading, label: "CTA final · Titular", widget: string }
          - { name: ctaParagraph, label: "CTA final · Párrafo", widget: text }
          - { name: ctaButtonLabel, label: "CTA final · Texto botón", widget: string }
          - { name: ctaButtonIcon, label: "CTA final · Icono botón", widget: string }
          - { name: ctaSecondaryLabel, label: "CTA final · Texto botón secundario", widget: string }
          - { name: ctaSecondaryHref, label: "CTA final · Enlace botón secundario", widget: string }

      - name: traffic-growth-en
        label: "Traffic & Growth (página dedicada, Inglés)"
        file: "src/content/pages/traffic-growth.en.json"
        format: json
        fields:
          - { name: title, label: "Título de pestaña (SEO)", widget: string }
          - { name: metaDescription, label: "Meta descripción (SEO)", widget: text }
          - { name: heroTag, label: "Hero · Etiqueta", widget: string }
          - { name: heroHeading, label: "Hero · Titular", widget: string }
          - { name: heroHighlight, label: "Hero · Palabra resaltada", widget: string, required: false }
          - { name: heroParagraph, label: "Hero · Párrafo", widget: text }
          - { name: heroCtaPrimaryLabel, label: "Hero · Botón principal", widget: string }
          - { name: heroCtaPrimaryHref, label: "Hero · Enlace botón principal", widget: string }
          - { name: heroCtaSecondaryLabel, label: "Hero · Botón secundario", widget: string }
          - { name: heroCtaSecondaryHref, label: "Hero · Enlace botón secundario", widget: string }
          - { name: modulesTag, label: "Módulos · Etiqueta", widget: string }
          - { name: modulesHeading, label: "Módulos · Titular", widget: string }
          - { name: modulesIntro, label: "Módulos · Párrafo", widget: text }
          - name: modules
            label: "Módulos (6: Meta Ads, Google Ads, SEO, Local SEO, Lead Gen, Growth Strategy)"
            widget: list
            fields:
              - { name: num, label: "Número (01, 02…)", widget: string }
              - { name: icon, label: "Icono", widget: string }
              - { name: title, label: "Título", widget: string }
              - { name: desc, label: "Descripción", widget: text }
              - { name: benefit, label: "Beneficio práctico", widget: text }
          - { name: processTag, label: "Proceso · Etiqueta", widget: string }
          - { name: processHeading, label: "Proceso · Titular", widget: string }
          - name: processSteps
            label: "Proceso · Pasos"
            widget: list
            fields:
              - { name: num, label: "Número (01, 02…)", widget: string }
              - { name: title, label: "Título", widget: string }
              - { name: desc, label: "Descripción", widget: text }
          - { name: fitTag, label: "Para quién · Etiqueta", widget: string }
          - { name: fitHeading, label: "Para quién · Titular", widget: string }
          - { name: fitIntro, label: "Para quién · Línea de posicionamiento local/global", widget: text }
          - name: fitItems
            label: "Para quién · Perfiles de negocio"
            widget: list
            fields:
              - { name: icon, label: "Icono", widget: string }
              - { name: title, label: "Título", widget: string }
              - { name: desc, label: "Descripción", widget: text }
          - { name: supportTag, label: "Servicios de apoyo · Etiqueta", widget: string }
          - { name: supportHeading, label: "Servicios de apoyo · Titular", widget: string }
          - { name: supportIntro, label: "Servicios de apoyo · Párrafo", widget: text }
          - name: supportItems
            label: "Servicios de apoyo · Enlaces (solo rutas reales existentes)"
            widget: list
            fields:
              - { name: icon, label: "Icono", widget: string }
              - { name: title, label: "Título", widget: string }
              - { name: href, label: "Enlace", widget: string }
          - { name: ctaTag, label: "CTA final · Etiqueta", widget: string }
          - { name: ctaHeading, label: "CTA final · Titular", widget: string }
          - { name: ctaParagraph, label: "CTA final · Párrafo", widget: text }
          - { name: ctaButtonLabel, label: "CTA final · Texto botón", widget: string }
          - { name: ctaButtonIcon, label: "CTA final · Icono botón", widget: string }
          - { name: ctaSecondaryLabel, label: "CTA final · Texto botón secundario", widget: string }
          - { name: ctaSecondaryHref, label: "CTA final · Enlace botón secundario", widget: string }

      # POSITIONING PHASE 4 — hub de Servicios de Apoyo. Solo copy propio
      # del hub (hero, callout de Traffic & Growth, categorías, CTA); las
      # tarjetas de servicio se siguen editando en "pages > home" (Inicio),
      # no se duplican aquí.
      - name: services-hub
        label: "Servicios (página hub)"
        file: "src/content/pages/services-hub.es.json"
        format: json
        fields:
          - { name: title, label: "Título de pestaña (SEO)", widget: string }
          - { name: metaDescription, label: "Meta descripción (SEO)", widget: text }
          - { name: heroTag, label: "Hero · Etiqueta", widget: string }
          - { name: heroHeading, label: "Hero · Titular", widget: string }
          - { name: heroParagraph, label: "Hero · Párrafo", widget: text }
          - { name: heroCtaPrimaryLabel, label: "Hero · Botón principal", widget: string }
          - { name: heroCtaPrimaryHref, label: "Hero · Enlace botón principal", widget: string }
          - { name: heroCtaSecondaryLabel, label: "Hero · Botón secundario", widget: string }
          - { name: heroCtaSecondaryHref, label: "Hero · Enlace botón secundario", widget: string }
          - { name: tgCalloutTag, label: "Callout Traffic & Growth · Etiqueta", widget: string }
          - { name: tgCalloutHeading, label: "Callout Traffic & Growth · Titular", widget: string }
          - { name: tgCalloutDesc, label: "Callout Traffic & Growth · Párrafo", widget: text }
          - { name: tgCalloutItems, label: "Callout Traffic & Growth · Ítems", widget: list, field: { name: item, label: "Ítem", widget: string } }
          - { name: tgCalloutCtaLabel, label: "Callout Traffic & Growth · Texto botón", widget: string }
          - { name: tgCalloutCtaHref, label: "Callout Traffic & Growth · Enlace (ruta real existente)", widget: string }
          - { name: digitalPresenceTag, label: "Presencia Digital · Etiqueta", widget: string }
          - { name: digitalPresenceIntro, label: "Presencia Digital · Párrafo", widget: text }
          - { name: contentBrandTag, label: "Contenido y Marca · Etiqueta", widget: string }
          - { name: contentBrandIntro, label: "Contenido y Marca · Párrafo", widget: text }
          - { name: paidAdsTag, label: "Publicidad Pagada · Etiqueta", widget: string }
          - { name: paidAdsNote, label: "Publicidad Pagada · Nota (superposición con Traffic & Growth)", widget: text }
          - { name: paidAdsNoteLinkLabel, label: "Publicidad Pagada · Texto del enlace de la nota", widget: string }
          - { name: automationTag, label: "Automatización y Sistemas · Etiqueta", widget: string }
          - { name: automationHeading, label: "Automatización y Sistemas · Titular", widget: string }
          - { name: automationBody, label: "Automatización y Sistemas · Párrafo (solo claims aprobados)", widget: text }
          - { name: automationCtaLabel, label: "Automatización y Sistemas · Texto botón", widget: string }
          - { name: automationCtaHref, label: "Automatización y Sistemas · Enlace", widget: string }
          - { name: sublimationNote, label: "Enlace a Sublimación · Texto", widget: text }
          - { name: sublimationCtaLabel, label: "Enlace a Sublimación · Texto botón", widget: string }
          - { name: sublimationCtaHref, label: "Enlace a Sublimación · Enlace (ruta real existente)", widget: string }
          - { name: ctaTag, label: "CTA final · Etiqueta", widget: string }
          - { name: ctaHeading, label: "CTA final · Titular", widget: string }
          - { name: ctaParagraph, label: "CTA final · Párrafo", widget: text }
          - { name: ctaButtonLabel, label: "CTA final · Texto botón", widget: string }
          - { name: ctaButtonIcon, label: "CTA final · Icono botón", widget: string }
          - { name: ctaSecondaryLabel, label: "CTA final · Texto botón secundario", widget: string }
          - { name: ctaSecondaryHref, label: "CTA final · Enlace botón secundario", widget: string }

      - name: services-hub-en
        label: "Servicios (página hub, Inglés)"
        file: "src/content/pages/services-hub.en.json"
        format: json
        fields:
          - { name: title, label: "Título de pestaña (SEO)", widget: string }
          - { name: metaDescription, label: "Meta descripción (SEO)", widget: text }
          - { name: heroTag, label: "Hero · Etiqueta", widget: string }
          - { name: heroHeading, label: "Hero · Titular", widget: string }
          - { name: heroParagraph, label: "Hero · Párrafo", widget: text }
          - { name: heroCtaPrimaryLabel, label: "Hero · Botón principal", widget: string }
          - { name: heroCtaPrimaryHref, label: "Hero · Enlace botón principal", widget: string }
          - { name: heroCtaSecondaryLabel, label: "Hero · Botón secundario", widget: string }
          - { name: heroCtaSecondaryHref, label: "Hero · Enlace botón secundario", widget: string }
          - { name: tgCalloutTag, label: "Callout Traffic & Growth · Etiqueta", widget: string }
          - { name: tgCalloutHeading, label: "Callout Traffic & Growth · Titular", widget: string }
          - { name: tgCalloutDesc, label: "Callout Traffic & Growth · Párrafo", widget: text }
          - { name: tgCalloutItems, label: "Callout Traffic & Growth · Ítems", widget: list, field: { name: item, label: "Ítem", widget: string } }
          - { name: tgCalloutCtaLabel, label: "Callout Traffic & Growth · Texto botón", widget: string }
          - { name: tgCalloutCtaHref, label: "Callout Traffic & Growth · Enlace (ruta real existente)", widget: string }
          - { name: digitalPresenceTag, label: "Presencia Digital · Etiqueta", widget: string }
          - { name: digitalPresenceIntro, label: "Presencia Digital · Párrafo", widget: text }
          - { name: contentBrandTag, label: "Contenido y Marca · Etiqueta", widget: string }
          - { name: contentBrandIntro, label: "Contenido y Marca · Párrafo", widget: text }
          - { name: paidAdsTag, label: "Publicidad Pagada · Etiqueta", widget: string }
          - { name: paidAdsNote, label: "Publicidad Pagada · Nota (superposición con Traffic & Growth)", widget: text }
          - { name: paidAdsNoteLinkLabel, label: "Publicidad Pagada · Texto del enlace de la nota", widget: string }
          - { name: automationTag, label: "Automatización y Sistemas · Etiqueta", widget: string }
          - { name: automationHeading, label: "Automatización y Sistemas · Titular", widget: string }
          - { name: automationBody, label: "Automatización y Sistemas · Párrafo (solo claims aprobados)", widget: text }
          - { name: automationCtaLabel, label: "Automatización y Sistemas · Texto botón", widget: string }
          - { name: automationCtaHref, label: "Automatización y Sistemas · Enlace", widget: string }
          - { name: sublimationNote, label: "Enlace a Sublimación · Texto", widget: text }
          - { name: sublimationCtaLabel, label: "Enlace a Sublimación · Texto botón", widget: string }
          - { name: sublimationCtaHref, label: "Enlace a Sublimación · Enlace (ruta real existente)", widget: string }
          - { name: ctaTag, label: "CTA final · Etiqueta", widget: string }
          - { name: ctaHeading, label: "CTA final · Titular", widget: string }
          - { name: ctaParagraph, label: "CTA final · Párrafo", widget: text }
          - { name: ctaButtonLabel, label: "CTA final · Texto botón", widget: string }
          - { name: ctaButtonIcon, label: "CTA final · Icono botón", widget: string }
          - { name: ctaSecondaryLabel, label: "CTA final · Texto botón secundario", widget: string }
          - { name: ctaSecondaryHref, label: "CTA final · Enlace botón secundario", widget: string }

      # POSITIONING PHASE 5 — página bespoke dedicada de Sublimación y
      # Productos con Marca (tercera línea de negocio primaria). Orientada
      # a solicitudes de cotización — sin inventario, precios fijos,
      # tiempos de entrega ni SKUs.
      - name: sublimation
        label: "Sublimación y Productos con Marca (página)"
        file: "src/content/pages/sublimation.es.json"
        format: json
        fields:
          - { name: title, label: "Título de pestaña (SEO)", widget: string }
          - { name: metaDescription, label: "Meta descripción (SEO)", widget: text }
          - { name: heroTag, label: "Hero · Etiqueta", widget: string }
          - { name: heroHeading, label: "Hero · Titular", widget: string }
          - { name: heroHighlight, label: "Hero · Palabra resaltada", widget: string, required: false }
          - { name: heroParagraph, label: "Hero · Párrafo", widget: text }
          - { name: heroCtaPrimaryLabel, label: "Hero · Botón principal (Solicitar Cotización)", widget: string }
          - { name: heroCtaPrimaryHref, label: "Hero · Enlace botón principal", widget: string }
          - { name: heroCtaSecondaryLabel, label: "Hero · Botón secundario", widget: string }
          - { name: heroCtaSecondaryHref, label: "Hero · Enlace botón secundario", widget: string }
          - { name: categoriesTag, label: "Categorías · Etiqueta", widget: string }
          - { name: categoriesHeading, label: "Categorías · Titular", widget: string }
          - name: categories
            label: "Categorías de productos (sin inventario/precios/especificaciones inventadas)"
            widget: list
            fields:
              - { name: num, label: "Número (01…06)", widget: string }
              - { name: icon, label: "Icono", widget: string }
              - { name: title, label: "Título", widget: string }
              - { name: desc, label: "Descripción", widget: text }
          - { name: fitTag, label: "Para quién · Etiqueta", widget: string }
          - { name: fitHeading, label: "Para quién · Titular", widget: string }
          - { name: fitIntro, label: "Para quién · Línea de posicionamiento local/global", widget: text }
          - { name: fitItems, label: "Para quién · Perfiles", widget: list, field: { name: item, label: "Perfil", widget: string } }
          - { name: processTag, label: "Cómo funciona · Etiqueta", widget: string }
          - { name: processHeading, label: "Cómo funciona · Titular", widget: string }
          - name: processSteps
            label: "Cómo funciona · Pasos"
            widget: list
            fields:
              - { name: num, label: "Número (01, 02…)", widget: string }
              - { name: title, label: "Título", widget: string }
              - { name: desc, label: "Descripción", widget: text }
          - { name: brandingTag, label: "Conexión con marketing · Etiqueta", widget: string }
          - { name: brandingHeading, label: "Conexión con marketing · Titular", widget: string }
          - { name: brandingBody, label: "Conexión con marketing · Párrafo", widget: text }
          - name: brandingLinks
            label: "Conexión con marketing · Enlaces (solo rutas reales existentes)"
            widget: list
            fields:
              - { name: label, label: "Texto", widget: string }
              - { name: href, label: "Enlace", widget: string }
          - { name: ctaTag, label: "CTA final · Etiqueta", widget: string }
          - { name: ctaHeading, label: "CTA final · Titular", widget: string }
          - { name: ctaParagraph, label: "CTA final · Párrafo", widget: text }
          - { name: ctaButtonLabel, label: "CTA final · Texto botón", widget: string }
          - { name: ctaButtonIcon, label: "CTA final · Icono botón", widget: string }
          - { name: ctaSecondaryLabel, label: "CTA final · Texto botón secundario", widget: string }
          - { name: ctaSecondaryHref, label: "CTA final · Enlace botón secundario", widget: string }

      - name: sublimation-en
        label: "Sublimación y Productos con Marca (página, Inglés)"
        file: "src/content/pages/sublimation.en.json"
        format: json
        fields:
          - { name: title, label: "Título de pestaña (SEO)", widget: string }
          - { name: metaDescription, label: "Meta descripción (SEO)", widget: text }
          - { name: heroTag, label: "Hero · Etiqueta", widget: string }
          - { name: heroHeading, label: "Hero · Titular", widget: string }
          - { name: heroHighlight, label: "Hero · Palabra resaltada", widget: string, required: false }
          - { name: heroParagraph, label: "Hero · Párrafo", widget: text }
          - { name: heroCtaPrimaryLabel, label: "Hero · Botón principal (Request a Quote)", widget: string }
          - { name: heroCtaPrimaryHref, label: "Hero · Enlace botón principal", widget: string }
          - { name: heroCtaSecondaryLabel, label: "Hero · Botón secundario", widget: string }
          - { name: heroCtaSecondaryHref, label: "Hero · Enlace botón secundario", widget: string }
          - { name: categoriesTag, label: "Categorías · Etiqueta", widget: string }
          - { name: categoriesHeading, label: "Categorías · Titular", widget: string }
          - name: categories
            label: "Categorías de productos (sin inventario/precios/especificaciones inventadas)"
            widget: list
            fields:
              - { name: num, label: "Número (01…06)", widget: string }
              - { name: icon, label: "Icono", widget: string }
              - { name: title, label: "Título", widget: string }
              - { name: desc, label: "Descripción", widget: text }
          - { name: fitTag, label: "Para quién · Etiqueta", widget: string }
          - { name: fitHeading, label: "Para quién · Titular", widget: string }
          - { name: fitIntro, label: "Para quién · Línea de posicionamiento local/global", widget: text }
          - { name: fitItems, label: "Para quién · Perfiles", widget: list, field: { name: item, label: "Perfil", widget: string } }
          - { name: processTag, label: "Cómo funciona · Etiqueta", widget: string }
          - { name: processHeading, label: "Cómo funciona · Titular", widget: string }
          - name: processSteps
            label: "Cómo funciona · Pasos"
            widget: list
            fields:
              - { name: num, label: "Número (01, 02…)", widget: string }
              - { name: title, label: "Título", widget: string }
              - { name: desc, label: "Descripción", widget: text }
          - { name: brandingTag, label: "Conexión con marketing · Etiqueta", widget: string }
          - { name: brandingHeading, label: "Conexión con marketing · Titular", widget: string }
          - { name: brandingBody, label: "Conexión con marketing · Párrafo", widget: text }
          - name: brandingLinks
            label: "Conexión con marketing · Enlaces (solo rutas reales existentes)"
            widget: list
            fields:
              - { name: label, label: "Texto", widget: string }
              - { name: href, label: "Enlace", widget: string }
          - { name: ctaTag, label: "CTA final · Etiqueta", widget: string }
          - { name: ctaHeading, label: "CTA final · Titular", widget: string }
          - { name: ctaParagraph, label: "CTA final · Párrafo", widget: text }
          - { name: ctaButtonLabel, label: "CTA final · Texto botón", widget: string }
          - { name: ctaButtonIcon, label: "CTA final · Icono botón", widget: string }
          - { name: ctaSecondaryLabel, label: "CTA final · Texto botón secundario", widget: string }
          - { name: ctaSecondaryHref, label: "CTA final · Enlace botón secundario", widget: string }

  # ─────────── SERVICIOS UNIFORMES (features + planes) ───────────
  - name: services
    label: "Servicios (Español)"
    label_singular: "Servicio"
    editor:
      preview: false
    folder: "src/content/services/es"
    format: json
    extension: json
    create: true
    identifier_field: title
    slug: "{{fields.title | slugify}}"
    summary: "{{order}} · {{title}}"
    sortable_fields: [order, title]
    fields:
      - { name: title, label: "Nombre", widget: string }
      - { name: order, label: "Orden", widget: number, value_type: int }
      - { name: icon, label: "Icono", widget: string }
      - { name: heroHeading, label: "Hero · Titular", widget: string }
      - { name: heroHighlight, label: "Hero · Palabra resaltada", widget: string, required: false }
      - { name: heroSub, label: "Hero · Subtítulo", widget: text }
      - { name: heroCtaLabel, label: "Hero · Texto botón", widget: string }
      - { name: image, label: "Imagen", widget: image }
      - { name: imageAlt, label: "Texto alternativo", widget: string }
      - name: features
        label: "Características"
        widget: list
        fields:
          - { name: icon, label: "Icono", widget: string }
          - { name: title, label: "Título", widget: string }
          - { name: desc, label: "Descripción", widget: text }
      - name: plans
        label: "Planes"
        widget: list
        fields:
          - { name: name, label: "Nombre", widget: string }
          - { name: sub, label: "Subtítulo", widget: string }
          - { name: price, label: "Precio", widget: string }
          - { name: featured, label: "¿Destacado?", widget: boolean, default: false }
          - { name: features, label: "Incluye", widget: list, field: { name: item, label: "Ítem", widget: string } }
          - { name: group, label: "Grupo (opcional — debe coincidir con una clave en 'Grupos de planes')", widget: string, required: false }
      - name: planGroups
        label: "Grupos de planes (opcional)"
        widget: list
        required: false
        fields:
          - { name: key, label: "Clave (debe coincidir con 'Grupo' en cada plan)", widget: string }
          - { name: label, label: "Título visible del grupo", widget: string }
      - { name: plansHeading, label: "Titular de precios (opcional — reemplaza 'Planes y precios')", widget: string, required: false }
      - { name: ctaHeading, label: "CTA · Titular", widget: string }
      - { name: ctaText, label: "CTA · Texto", widget: text }
      - { name: ctaButtonLabel, label: "CTA · Texto botón", widget: string }
      - { name: ctaButtonIcon, label: "CTA · Icono botón", widget: string }

  - name: services-en
    label: "Servicios (Inglés)"
    label_singular: "Service"
    editor:
      preview: false
    folder: "src/content/services/en"
    format: json
    extension: json
    create: true
    identifier_field: title
    slug: "{{fields.title | slugify}}"
    summary: "{{order}} · {{title}}"
    sortable_fields: [order, title]
    fields:
      - { name: title, label: "Nombre (Inglés)", widget: string }
      - { name: urlSlug, label: "URL en inglés (p.ej. web-development)", widget: string }
      - { name: order, label: "Orden", widget: number, value_type: int }
      - { name: icon, label: "Icono", widget: string }
      - { name: heroHeading, label: "Hero · Titular", widget: string }
      - { name: heroHighlight, label: "Hero · Palabra resaltada", widget: string, required: false }
      - { name: heroSub, label: "Hero · Subtítulo", widget: text }
      - { name: heroCtaLabel, label: "Hero · Texto botón", widget: string }
      - { name: image, label: "Imagen", widget: image }
      - { name: imageAlt, label: "Texto alternativo", widget: string }
      - name: features
        label: "Características"
        widget: list
        fields:
          - { name: icon, label: "Icono", widget: string }
          - { name: title, label: "Título", widget: string }
          - { name: desc, label: "Descripción", widget: text }
      - name: plans
        label: "Planes"
        widget: list
        fields:
          - { name: name, label: "Nombre", widget: string }
          - { name: sub, label: "Subtítulo", widget: string }
          - { name: price, label: "Precio", widget: string }
          - { name: featured, label: "¿Destacado?", widget: boolean, default: false }
          - { name: features, label: "Incluye", widget: list, field: { name: item, label: "Ítem", widget: string } }
          - { name: group, label: "Grupo (opcional — debe coincidir con una clave en 'Grupos de planes')", widget: string, required: false }
      - name: planGroups
        label: "Grupos de planes (opcional)"
        widget: list
        required: false
        fields:
          - { name: key, label: "Clave (debe coincidir con 'Grupo' en cada plan)", widget: string }
          - { name: label, label: "Título visible del grupo", widget: string }
      - { name: plansHeading, label: "Titular de precios (opcional — reemplaza 'Planes y precios')", widget: string, required: false }
      - { name: ctaHeading, label: "CTA · Titular", widget: string }
      - { name: ctaText, label: "CTA · Texto", widget: text }
      - { name: ctaButtonLabel, label: "CTA · Texto botón", widget: string }
      - { name: ctaButtonIcon, label: "CTA · Icono botón", widget: string }

  # ─────────── TESTIMONIOS (bilingüe, P2C-2a — infraestructura) ───────────
  # Ambas colecciones arrancan vacías a propósito: nada se publica en el
  # sitio hasta que un testimonio real se cargue aquí Y se marque
  # "¿Publicado?" — ver el campo `published` y su default en content.config.ts.
  - name: testimonials
    label: "Testimonios (Español)"
    label_singular: "Testimonio"
    editor:
      preview: false
    folder: "src/content/testimonials/es"
    format: json
    extension: json
    create: true
    identifier_field: clientName
    slug: "{{fields.clientName | slugify}}"
    summary: "{{clientName}} · {{businessName}}"
    fields:
      - { name: clientName, label: "Nombre del cliente", widget: string }
      - { name: businessName, label: "Negocio", widget: string, required: false }
      - { name: quote, label: "Testimonio", widget: text }
      - { name: rating, label: "Calificación (1 a 5)", widget: number, value_type: int, min: 1, max: 5, required: false }
      - { name: photo, label: "Foto", widget: image, required: false }
      - { name: relatedService, label: "Servicio relacionado (id, p.ej. desarrollo-web)", widget: string, required: false }
      - { name: featured, label: "¿Destacado?", widget: boolean, default: false }
      - { name: published, label: "¿Publicado? (no aparece en el sitio hasta marcar esto)", widget: boolean, default: false }

  - name: testimonials-en
    label: "Testimonials (Inglés)"
    label_singular: "Testimonial"
    editor:
      preview: false
    folder: "src/content/testimonials/en"
    format: json
    extension: json
    create: true
    identifier_field: clientName
    slug: "{{fields.clientName | slugify}}"
    summary: "{{clientName}} · {{businessName}}"
    fields:
      - { name: clientName, label: "Client Name", widget: string }
      - { name: businessName, label: "Business", widget: string, required: false }
      - { name: quote, label: "Testimonial", widget: text }
      - { name: rating, label: "Rating (1 to 5)", widget: number, value_type: int, min: 1, max: 5, required: false }
      - { name: photo, label: "Photo", widget: image, required: false }
      - { name: relatedService, label: "Related service (id, e.g. web-development)", widget: string, required: false }
      - { name: featured, label: "Featured?", widget: boolean, default: false }
      - { name: published, label: "Published? (won't appear on the site until this is checked)", widget: boolean, default: false }

  # ─────────── PORTFOLIO / TRABAJOS (bilingüe, P2C-2b — infraestructura) ───
  # Ambas colecciones arrancan vacías a propósito: nada se publica en el
  # sitio hasta que un proyecto real se cargue aquí Y se marquen
  # "¿Publicado?" Y "¿Consentimiento del cliente?" — ver los campos
  # `published`/`clientConsent` y sus defaults en content.config.ts. La
  # plantilla pública solo renderiza un proyecto cuando AMBOS son true.
  - name: portfolio
    label: "Portfolio / Trabajos (Español)"
    label_singular: "Trabajo"
    editor:
      preview: false
    folder: "src/content/portfolio/es"
    format: json
    extension: json
    create: true
    identifier_field: title
    slug: "{{fields.title | slugify}}"
    summary: "{{title}} · {{category}}"
    fields:
      - { name: title, label: "Título del proyecto", widget: string }
      - { name: category, label: "Categoría (p.ej. Diseño de Marca)", widget: string }
      - { name: summary, label: "Resumen", widget: text }
      - { name: clientName, label: "Nombre del cliente", widget: string, required: false }
      - { name: relatedService, label: "Servicio relacionado (id, p.ej. desarrollo-web)", widget: string, required: false }
      - { name: heroImage, label: "Imagen principal", widget: image, required: false }
      - { name: heroImageAlt, label: "Imagen principal · Texto alternativo", widget: string, required: false }
      - name: galleryImages
        label: "Galería (opcional)"
        widget: list
        required: false
        fields:
          - { name: image, label: "Imagen", widget: image }
          - { name: alt, label: "Texto alternativo", widget: string }
      - { name: projectUrl, label: "Enlace al proyecto (opcional)", widget: string, required: false }
      - { name: outcome, label: "Resultado (texto libre, opcional — sin métricas inventadas)", widget: string, required: false }
      - { name: featured, label: "¿Destacado?", widget: boolean, default: false }
      - { name: published, label: "¿Publicado? (no aparece en el sitio hasta marcar esto)", widget: boolean, default: false }
      - { name: clientConsent, label: "¿Consentimiento del cliente para publicar? (obligatorio junto con \"¿Publicado?\" para aparecer en el sitio)", widget: boolean, default: false }

  - name: portfolio-en
    label: "Portfolio / Selected Work (Inglés)"
    label_singular: "Project"
    editor:
      preview: false
    folder: "src/content/portfolio/en"
    format: json
    extension: json
    create: true
    identifier_field: title
    slug: "{{fields.title | slugify}}"
    summary: "{{title}} · {{category}}"
    fields:
      - { name: title, label: "Project Title", widget: string }
      - { name: category, label: "Category (e.g. Brand Design)", widget: string }
      - { name: summary, label: "Summary", widget: text }
      - { name: clientName, label: "Client Name", widget: string, required: false }
      - { name: relatedService, label: "Related service (id, e.g. web-development)", widget: string, required: false }
      - { name: heroImage, label: "Hero Image", widget: image, required: false }
      - { name: heroImageAlt, label: "Hero Image · Alt Text", widget: string, required: false }
      - name: galleryImages
        label: "Gallery (optional)"
        widget: list
        required: false
        fields:
          - { name: image, label: "Image", widget: image }
          - { name: alt, label: "Alt Text", widget: string }
      - { name: projectUrl, label: "Project Link (optional)", widget: string, required: false }
      - { name: outcome, label: "Outcome (free text, optional — no invented metrics)", widget: string, required: false }
      - { name: featured, label: "Featured?", widget: boolean, default: false }
      - { name: published, label: "Published? (won't appear on the site until this is checked)", widget: boolean, default: false }
      - { name: clientConsent, label: "Client consent to publish? (required together with \"Published?\" to appear on the site)", widget: boolean, default: false }
